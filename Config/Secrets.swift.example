import Foundation

// Copy this file to `Secrets.swift` (same directory) and fill in real values.
// `Secrets.swift` is gitignored — never commit it.
//
// To obtain your Canvas feed URL:
//   1. Log into Canvas
//   2. Open Calendar
//   3. Click "Calendar Feed" in the right sidebar
//   4. Copy the URL — it'll end in .ics

enum Secrets {
    static let canvasFeedURL = ""  // "https://yourschool.instructure.com/feeds/calendars/user_TOKEN.ics"
}
